# apiVersion: apps/v1
# kind: Deployment
# metadata:
#   name: frontend
# spec:
#   replicas: 2
#   selector:
#     matchLabels:
#       app: frontend
#   template:
#     metadata:
#       labels:
#         app: frontend
#     spec:
#       containers:
#         - name: frontend
#           image: gcr.io/gtmlabs/frontend:{{TAG}}
#           ports:
#             - containerPort: 3000
#           # envFrom:
#           #   - configMapRef:
#           #       name: frontend-config


apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: 730335385079.dkr.ecr.ap-south-1.amazonaws.com/frontend-ecr-repo:{{TAG}}
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: frontend-config
